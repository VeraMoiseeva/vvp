#include <iostream>
using namespace std;

int main()
{
    int a,d,b;
    setlocale(0, "");
    cout << "Введите значения a \n";
    cin >> a;
    d=a*a;
    b = a * a * a * a * a;
    cout << "A^15=";
    cout << d*d*d*d*d*b;
   
}
